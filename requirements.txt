# Migração CORE from ERP — dependências Python
# Instalação: pip install -r requirements.txt
#
# Requisitos de sistema (não instaláveis via pip):
# - ODBC Driver 17 for SQL Server (pyodbc / SQL Server origem)
# - PostgreSQL client tools (pg_dump) — opcional, scripts em clone_tables/

pandas==2.3.3
numpy==1.26.4
openpyxl==3.1.5
psycopg2-binary==2.9.11
pyodbc==5.0.1
